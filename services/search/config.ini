; /etc/typesense/typesense-server.ini

[server]
data-dir = /data ; ensure that this is what the docker volume is mounted to
enable-cors = true
# cors-domains = https://docs.blink.rest ; for now, disable it so I can access it from the native application
log-slow-requests-time-ms = 1000
