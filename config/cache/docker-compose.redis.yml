version: "3"

services:
  redis:
    image: redis:6-alpine
    container_name: redis
    restart: always
    networks:
      - private
    expose:
      - 6379

networks:
  private:
    external: true
