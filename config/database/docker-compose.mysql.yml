version: '3'

services:
  mysql:
    image: mysql:8
    container_name: mysql
    restart: always
    networks:
      - private
    expose:
      - 3306
    volumes:
      - ./volumes/mysql-dump:/mnt/dump
      - ./config/database/init:/docker-entrypoint-initdb.d
      - ./config/database/my.cnf:/etc/mysql/conf.d/my.cnf
      - ./config/database/dump.sh:/usr/local/bin/dump
      - ./config/database/restore.sh:/usr/local/bin/restore
    environment:
      MYSQL_ROOT_PASSWORD: mysql-root-password
    cap_add:
      - SYS_NICE
    labels:
      com.datadoghq.ad.check_names: '["mysql"]'
      com.datadoghq.ad.init_configs: '[{}]'
      com.datadoghq.ad.instances: >
        [{
          "server": "mysql",
          "user": "datadog",
          "pass": "datadog-password",
          "options": {
            "extra_status_metrics": true,
            "extra_innodb_metrics": true,
            "extra_performance_metrics": true,
            "schema_size_metrics": true
          }
        }]
      com.datadoghq.ad.logs: '[{"source": "mysql", "service": "mysql"}]'
      com.centurylinklabs.watchtower.enable: 'true'
      com.centurylinklabs.watchtower.lifecycle.pre-update: dump
      com.centurylinklabs.watchtower.lifecycle.post-update: restore

networks:
  private:
    external: true
