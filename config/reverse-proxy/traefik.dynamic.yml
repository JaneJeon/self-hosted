tls:
  certificates:
    - keyFile: /ets/letsencrypt/live/janejeon.dev/privkey.pem
      certFile: /ets/letsencrypt/live/janejeon.dev/fullchain.pem
    - keyFile: /ets/letsencrypt/live/nariahn.com/privkey.pem
      certFile: /ets/letsencrypt/live/nariahn.com/fullchain.pem
    - keyFile: /ets/letsencrypt/live/homelab-wildcard/privkey.pem
      certFile: /ets/letsencrypt/live/homelab-wildcard/fullchain.pem

http:
  middlewares:
    floc:
      headers:
        customResponseHeaders:
          Permissions-Policy: interest-cohort=()
    gzip:
      compress: {}
    no-www: # stripping https://www.blah -> https://blah
      redirectRegex:
        regex: ^(?:https://)?(?:www\.)?(.+)
        replacement: https://${1}
        permanent: true
    sso-proxy:
      forwardauth:
        address: http://authelia:9091/api/verify?rd=https://auth.janejeon.com
        trustForwardHeader: true
        authResponseHeaders:
          - Remote-User
          - Remote-Groups
          - Remote-Name
          - Remote-Email
