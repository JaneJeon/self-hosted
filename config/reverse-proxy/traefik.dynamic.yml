http:
  middlewares:
    floc:
      headers:
        customResponseHeaders:
          Permissions-Policy: interest-cohort=()
    gzip:
      compress: {}
    https-www-strip: # stripping https://www.blah -> https://blah
      redirectRegex:
        regex: ^(?:https)?://(?:www\.)?(.+)
        replacement: https://$${1}
        permanent: true
